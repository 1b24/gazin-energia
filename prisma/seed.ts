/**
 * Seed stub. Implementation pending — see BRIEF.md.
 */
export {};

async function main() {
  // intentionally empty
}

main()
  .then(() => process.exit(0))
  .catch((err) => {
    console.error(err);
    process.exit(1);
  });
